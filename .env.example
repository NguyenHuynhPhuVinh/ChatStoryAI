# Cấu hình MySQL
MYSQL_HOST=             # Địa chỉ host của MySQL server (sử dụng tên service trong Docker)
MYSQL_PORT=              # Port của MySQL server (mặc định: 3306)
MYSQL_USER=    # Tên người dùng MySQL
MYSQL_PASSWORD=  # Mật khẩu MySQL
MYSQL_ROOT_PASSWORD=  # Mật khẩu root MySQL
MYSQL_DATABASE=   # Tên cơ sở dữ liệu MySQL

# Cấu hình SSL cho MySQL
MYSQL_SSL_ENABLED=       # Bật SSL cho kết nối MySQL (true/false)
MYSQL_SSL_REJECT_UNAUTHORIZED=  # Cho phép chứng chỉ tự ký (false) hoặc yêu cầu chứng chỉ hợp lệ (true)
MYSQL_SSL_CA=            # Đường dẫn đến file CA certificate (tùy chọn)
MYSQL_SSL_CERT=          # Đường dẫn đến file client certificate (tùy chọn)
MYSQL_SSL_KEY=           # Đường dẫn đến file client private key (tùy chọn)

# Cấu hình NextAuth
NEXTAUTH_SECRET=             # Khóa bí mật cho NextAuth, dùng để mã hóa session
NEXTAUTH_URL=                # URL của ứng dụng (ví dụ: http://localhost:3000)
NEXT_PUBLIC_URL=             # URL công khai của ứng dụng

# Cấu hình Google Service Account
GOOGLE_CLIENT_EMAIL=         # Email của Google Service Account
GOOGLE_PRIVATE_KEY=          # Khóa riêng tư của Google Service Account
GOOGLE_DRIVE_FOLDER_ID=      # ID thư mục Google Drive để lưu trữ file

# Cấu hình Gmail
GMAIL_USER=                  # Địa chỉ email Gmail
GMAIL_APP_PASSWORD=          # Mật khẩu ứng dụng Gmail

# Cấu hình Google OAuth
GOOGLE_CLIENT_ID=            # ID ứng dụng Google OAuth
GOOGLE_CLIENT_SECRET=        # Khóa bí mật ứng dụng Google OAuth

# Cấu hình VNPay
VNP_TMN_CODE=                # Mã merchant VNPay
VNP_HASH_SECRET=             # Khóa bí mật VNPay

# Cấu hình API AI
GEMINI_API_KEY=              # Khóa API của Google Gemini
TOGETHER_API_KEY=            # Khóa API của Together AI
